#!/usr/bin/env node

var RedisHook = require('../lib/redis').RedisHook;

var redis = new RedisHook({
  "name": 'the-redis-hook',
  "debug": true,
  "redis": {
    "port": 6379,
    "host": "127.0.0.1",
    "options": {}
  }
});

redis.start();
